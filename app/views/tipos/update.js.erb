$('#modalTipos').modal('toggle');
$('#tipo_<%= @tipo.id %>').replaceWith(
    '<tr id="tipo_<%= @tipo.id %>" ><td><%= @tipo.descricao %></td><td><%= link_to 'editar', edit_tipo_path(@tipo), html_options = {class: "btn btn-info btn-xs", remote: true} %> <%= link_to "Deletar", @tipo, remote: true, method: :delete, data: { confirm: "Deleta Descrição?: #{@tipo.descricao}?"}, class: "btn btn-danger btn-xs" %></td></tr>'
);